import Layout from '../Layout/Layout';

const NewAdvertPage = props => {
  return (
    <Layout title="Publish your advert!" {...props}>
      <div>New Advert Page</div>
    </Layout>
  );
};

export default NewAdvertPage;
